import Foundation

enum Config {
    /// Base URL of your Railway deployment — no trailing slash
    static let apiBaseURL = "https://your-app.up.railway.app"

    /// Clerk publishable key (same one used in the web app)
    static let clerkPublishableKey = "pk_test_ZmVhc2libGUtb..."
}
